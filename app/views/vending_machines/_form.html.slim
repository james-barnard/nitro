= simple_form_for(@vending_machine) do |f|
  = f.error_notification

  .form-inputs
    = f.input :model
    = f.input :device_id
    = f.input :firmware_version
    = f.input :mode, label: 'Vend mode', as: :radio_buttons
    = f.association :location
    = f.association :route
    = f.input :owner
    = f.input :last_serviced

  .form-actions
    = f.button :submit
